---

## name: gold-accumulation-trading
description: >
  中国黄金积存金短线交易AI智能分析工具。实时搜集消息面与行情数据，进行小时级别市场情绪判断（看好/看跌/中性），
  可抓取京东金融金友圈「观点精选」作社区共识参考；基于多维技术指标（MA均线、BOLL布林带、MACD、KDJ、RSI、订单流）
  计算压力位/支撑位/轴心位，按消息面/价位/技术信号三轴综合后再用短线信号作执行门控，生成黄金买卖策略，
  并将现货金价转换为浙商积存金、民生积存金等银行产品的实际交易点位。
  使用场景：当用户需要黄金短线交易策略、金价技术分析、积存金买卖点位建议、市场情绪判断、
  压力支撑位分析时。关键词：黄金、积存金、金价分析、浙商积存金、民生积存金、短线交易、
  压力位、支撑位、轴心位、黄金技术分析、买入卖出点位、市场情绪、XAUUSD、金友圈

# 黄金积存金短线交易智能分析

面向中国积存金短线交易者的AI分析工具，覆盖消息面汇总→技术分析→交易策略→积存金点位换算全流程。

## 核心定位与目标

- **交易节奏**: 小时/天级别短线，快进快出，利用黄金波动赚差价
- **数据原则**: 越实时越好，数据延迟=判断失效，必须标注每条数据的获取时间
- **收益目标**: 通过多次高胜率操作复利积累，目标资产翻倍
- **积存金特点**: 无杠杆，每次涨跌约1-3%，需选择波动大的窗口（地缘事件、重大数据发布日）入场，买卖价差约0.5-1元/克需计入成本

## 工作流程



**路径约定**：下文 Python 命令中的 `<SkillRoot>` 均指本技能包根目录（含 `SKILL.md` 与 `scripts/`，目录名常为 `Trade-Accumulated-Gold-CN`）。Windows 示例：`D:\AI Workspace\finance agent\.agents\skills\Trade-Accumulated-Gold-CN`。执行时将 `<SkillRoot>` 替换为实际绝对路径。

用户触发后按以下4个阶段顺序执行：

### 阶段1: 消息面搜集与市场情绪判断

**目标**: 小时级别消息面汇总，输出市场情绪标签

1. 使用 `web_search` 搜集最近1-4小时黄金相关消息：
  - **宏观经济**: 美联储利率决议、非农数据、CPI/PPI、GDP、失业率
  - **地缘政治**: 军事冲突、贸易摩擦、制裁动态
  - **央行动态**: 各国央行购金/售金、外汇储备变动
  - **美元指数**: DXY走势、美债收益率变化
  - **市场资金**: ETF持仓变动(SPDR GLD)、COMEX持仓报告
  - **国内市场**: 上海金交所动态、央行黄金储备、人民币汇率
2. 对每条消息判定情感倾向：
  - **利多黄金**(+): 美联储降息预期、地缘冲突升级、美元走弱、避险情绪升温、央行增持
  - **利空黄金**(−): 美联储加息/缩表、风险偏好回升、美元走强、通胀数据降温
  - **中性**(0): 数据符合预期、无重大变化
3. 综合判定市场情绪标签：


| 条件          | 情绪标签        | 操作倾向    |
| ----------- | ----------- | ------- |
| 利多信号 ≥ 60%  | 🟢 **看好黄金** | 逢低做多为主  |
| 利空信号 ≥ 60%  | 🔴 **看跌黄金** | 逢高减仓/观望 |
| 利多利空均 < 60% | 🟡 **中性震荡** | 区间操作/轻仓 |


1. **京东金融「金友圈」最新观点精选（社区共识，与阶段1并列的定性输入）**

**目标**: 抓取[@黄金小小彬](https://roma.jd.com/content/personal?contentId=jimu_user_info-12066789&romaFileName=pageCommunityPersonal)个人动态流**最新一条**的金圈博主观点汇总文本，作为多博主共识参考；**失败不阻塞**后续阶段2/3，但须在报告中如实标注。

- **运行脚本**（依赖 `playwright` + `chromium`，见脚本文件头注释）：
  ```
  python "<SkillRoot>/scripts/fetch_jd_personal_latest_post.py"
  ```
- **解析 stdout JSON**：
  - `status == "ok"` → 记录 `title_text`、`content_id`、`fetched_at`；**禁止**逐字粘贴超长 `title_text` 进报告正文（除非用户明确要求），应提炼 **3–8 条**要点（关键价位、多空比例、共识关键词等）。
  - `status != "ok"` → 在报告中写「金友圈抓取失败：{error}」；**不得编造**金友圈正文。
- **与阶段1的对照用法**（供阶段3「AxisM」使用）：
  - 与公开消息情绪**一致** → 可在综合结论中适度加强区间/方向判断（仍须过三轴与执行门控）。
  - **严重冲突**（例如阶段1偏空而金友圈强烈偏多）→ 必须在「综合交易建议」中写明「公开消息/社区共识分歧」及更保守的仓位/等待条件。
- **溯源**：阶段4「五、数据来源」须增加一行：京东金融个人页 URL + 动态 `content_id` + `fetched_at`。

### 阶段2: 技术分析 — 压力位·支撑位·轴心位

**目标**: 基于实时行情计算关键点位

1. **获取实时数据**（严格按优先级顺序，**每步成功即停止，不重复执行后续步骤**）：

> ⚡ 核心原则：三种方式是**互斥的降级备选**，而非全部执行。第一步成功则直接进入技术分析阶段，不再执行第二、三步。

---

**【数据获取决策流程】**

```
STEP 1: 运行 Python 脚本获取 XAUUSD + 银行报价
         ↓
   [评估1] fetch_bank_quotes.py 中 banks.zheshang.status == "ok"
   并且    banks.minsheng.status == "ok"（Playwright XPath 成功）？
         ↓ YES → ✅ 使用脚本结果，跳过 STEP 2 和 STEP 3，进入技术分析
         ↓ NO  → 继续 STEP 2
         ↓
   [评估2] 脚本输出中是否有有效的 jd_latest_price（JD API 兜底价格）？
         ↓ YES → ⚠️ 使用 JD API 兜底价格并标注"非银行实盘成交价"，跳过 STEP 3，进入技术分析
         ↓ NO  → 继续 STEP 3
         ↓
STEP 3: 使用内置浏览器 MCP 直接导航银行页面（最终兜底）
         ↓
   [评估3] MCP 浏览器成功读取到价格数字？
         ↓ YES → ✅ 使用 MCP 页面价格，进入技术分析
         ↓ NO  → 使用搜索/Au99.99代理，并标注数据来源等级
```

---

**🥇 STEP 1 — Python 脚本（首选，两条命令可并行运行）**:

```
XAUUSD + USD/CNY 汇率：
`python "<SkillRoot>/scripts/live_market_data.py"`

浙商/民生银行积存金报价：
`python "<SkillRoot>/scripts/fetch_bank_quotes.py"`
```

执行后立即检查 `fetch_bank_quotes.py` 的 JSON 输出：

- `banks.zheshang.status == "ok"` 且 `banks.minsheng.status == "ok"` → **✅ 数据完整，直接使用，不再执行 STEP 2/3**
- 任意一个 `status == "error"` 但 `jd_latest_price` 存在有效价格 → **⚠️ 降级使用 JD API 兜底价并标注，不再执行 STEP 3**
- 两者均失败 → **进入 STEP 3**

**🥈 STEP 3 — 内置浏览器 MCP（仅当 STEP 1 完全失败时执行）**:

```
使用 MCP cursor-ide-browser 工具，依次导航：
- 浙商: https://m.jdjygold.com/finance-gold/gold-standard/home/?productSku=1961543816
- 民生: https://m.jdjygold.com/finance-gold/newgold/home/

读取页面中"实时金价(元/克)"字段，记录截图时间。
成功读取后立即使用，不再执行搜索兜底。
```

> ⚡ 这一步是短线交易的核心，必须执行，获取 Bid/Ask 实时价和多时间框架信号。

**搜索A — USD/CNY汇率（实时即期汇率）**:

```
web_search: "美元兑人民币 今日汇率" 或 "USD CNY spot rate today"
获取: 银行间即期汇率（非中间价）
注意: 当美元指数DXY大幅波动时（>0.5%），须使用即期汇率而非中间价，否则积存金换算偏差可达10-20元/克
```

**搜索B — 积存金报价（⚠️ 仅当上方 STEP 1 和 STEP 3 均未能获取到有效银行报价时才执行）**:

```
降级条件: Python 脚本失败 AND MCP 浏览器也未能读取价格
才执行以下搜索:

优先搜索: "浙商积存金 民生积存金 今日金价" 或 "积存金实时报价"
备选搜索: "上海黄金交易所 Au99.99 最新价" 或访问 sge.com.cn 延时行情
获取: 浙商黄金报价、民生黄金报价（或Au99.99价格作为代理）

注意:
1. 搜索摘要中的价格须带时间戳，拒绝使用"1天前"等过期数据
2. 若获取到的是 Au99.99 代理价，须在报告中注明偏差范围 ±5元/克
```

**搜索C — 黄金TD价格（辅助验证溢价）**:

```
web_search: "黄金TD Au(T+D) 实时价格"
获取: 黄金TD价格，用于交叉验证国内金价水平
```

1. **解读多时间框架技术信号**（从 Investing.com 获取，为短线入场提供关键依据）：


| 信号级别       | 用途       | 操作含义                             |
| ---------- | -------- | -------------------------------- |
| **30分钟信号** | 判断当前入场时机 | 强烈卖出/卖出 → 不入场，等待；买入/强烈买入 → 可考虑入场 |
| **1小时信号**  | 确认短线方向   | 与30分钟共振时信号最强                     |
| **5小时信号**  | 判断日内趋势   | 决定今天整体做多还是做空                     |
| **日线/周线**  | 确认大方向    | 顺大趋势操作，逆势要轻仓                     |


**多时间框架信号解读规则**:

- 30min + 1H 均为卖出/强烈卖出 → **⛔ 不入场，等支撑**
- 30min + 1H 均为买入/强烈买入 → **✅ 可以入场**
- 30min 卖出 但 5H/日线 买入 → **📊 中线看多，短线等回调至支撑后买**
- 各级别信号矛盾 → **🔄 轻仓或观望**

1. 计算压力位(R1/R2/R3)和支撑位(S1/S2/S3)，依据详见 [technical-analysis.md](references/technical-analysis.md)：

**压力位判断依据（5维）**:

- 近期高点、前期成交密集区
- 均线MA、布林带BOLL上轨压力
- MACD、KDJ、RSI指标共振位
- 实盘订单流与资金博弈阻力区
- 短期趋势线与形态颈线位

**支撑位判断依据（5维）**:

- 近期回调低点、前低支撑区域
- 关键均线、布林带下轨支撑
- 指标超卖区域与多头承接位
- 实盘买盘集中进场价位
- 趋势线、箱体下沿技术结构位

**统一原则**: 所有点位结合盘面实时走势，不硬套固定公式、跟着当下资金情绪走，点位精简实用只给关键有效位置。

1. 计算轴心位：

采用自定义算法（更贴合当下市场结构）：

```
P(轴心) = (R1 + S1) ÷ 2

其中:
  R1 = 第一压力位（最小数值压力位）
  S1 = 第一支撑位（最大数值支撑位）

盘中补充算法（使用今日已确认的实际高低点）:
  P(盘中轴心) = (今日实际最高价 + 今日实际最低价) ÷ 2
  → 盘中两种轴心都展示，互相印证
```

**轴心位用途**:

- **判断强弱**: 金价在P之上 → 短期多头占优；金价在P之下 → 短期空头占优
- **交易参考**: 价格靠近轴心位 → 观察多空博弈；有效突破/跌破 → 新趋势确立
- **策略锚点**: 逢高减仓和逢低补仓的参考坐标，规划交易区间和风险边界

同时提供经典轴心位供对照（参见 [technical-analysis.md](references/technical-analysis.md)）。

### 阶段3: 交易策略生成与积存金点位换算

**目标**: 在**三轴综合结论**之上，用 **30min/1H 作为执行门控**，输出可执行的买卖策略与积存金点位；避免「仅由短线信号定方向」。

#### 3.1 三轴定性（先各给一档：`偏多` / `偏空` / `震荡`）


| 轴         | 代号        | 输入来源                                        | 定性规则（执行时逐条核对，简明写入报告）                                                            |
| --------- | --------- | ------------------------------------------- | ------------------------------------------------------------------------------- |
| **消息面轴**  | **AxisM** | 阶段1 情绪标签 + 金友圈提炼要点（若抓取失败则仅用阶段1）             | 🟢看好→偏多；🔴看跌→偏空；🟡中性→震荡；与金友圈严重冲突时 AxisM 标为 **震荡** 并在综合结论中说明分歧                   |
| **价位结构轴** | **AxisL** | 现价 vs **P（盘中轴心）**、**R1**、**S1**（阶段2已算出的关键带） | 现价明显高于 P 且更接近 R1 而非 S1 → **偏多**；明显低于 P 且更接近 S1 → **偏空**；夹在 P±窄幅或反复穿越 P → **震荡** |
| **技术信号轴** | **AxisS** | 多时间框架信号（**须至少引用 5H 或日线** 是否与 30m/1H 同向）     | 30m/1H 与 5H/日线 同向 → 与短线一致标 **偏多/偏空**；30m/1H 与 5H/日线 反向 → 标 **震荡** 并注明「短线逆势/噪音」  |


#### 3.2 综合档位与冲突处理（写入报告「综合交易建议」）

- **三轴一致（偏多或偏空）**：方向明确；仍须满足下方「执行门控」与 R:R 方可建议动手。
- **两轴一致、一轴背离**：输出 **条件单式** 建议（示例：AxisM+AxisL 偏多，但 30m 卖出 → **不追多**，仅 **回调至 S1/S2 带且门控转好** 再试；禁止与门控矛盾的「立即满仓」）。
- **三轴互斥或两轴互斥**：默认 **观望 / 轻仓区间**；必须写出**主导矛盾**及优先级：**突发风险与重大日程（AxisM 中的硬事件） > 日线/周线趋势（AxisS 大级别） > 30m/1H 短线信号（执行噪音）**。
- **原场景表（仅作辅助，不得绕过三轴）**：当三轴与下表冲突时，**以三轴结论为准**，下表仅用于细化话术。


| 场景                                 | 策略建议                            |
| ---------------------------------- | ------------------------------- |
| 情绪看好 + 价格靠近S1/S2 + **30min/1H 买入** | 💰 **分批买入**（须在综合档位非互斥、且门控允许时）   |
| 情绪看好 + 价格在轴心上方 + **30min/1H 买入**   | ✅ **可试多**，设好止损；若 AxisS 为震荡则降为轻仓 |
| 情绪看好 + 价格在轴心上方 + **30min/1H 卖出**   | ⏳ **等回调**，不追高，等价格回调至S1 且门控改善    |
| 情绪看好 + 价格从高点大幅回落 + **30min 强烈卖出**  | ⛔ **不抢反弹**，等支撑区止稳 + 门控转好        |
| 情绪看跌 + 价格靠近R1/R2                   | 🔻 **逢高减仓**，在压力位附近卖出            |
| 情绪中性 + 价格区间震荡                      | 🔄 **区间操作**，支撑买/压力卖，轻仓          |


#### 3.3 执行门控（30min/1H）与「综合交易建议」的关系

- **执行门控**只回答：**当下时点是否适合下单**（过滤器），**不单独回答**「本周偏多还是偏空」。
- **综合交易建议**必须 **一句话先写三轴综合档位**，再写门控允许的**动作**（可动手 / 只等待 / 只减仓）：门控为「不入场」时，若 AxisM+AxisL 仍偏多，应写 **「中期偏多但短线不追，等待 S× 与信号」**，禁止只写「强烈买入」之类与门控矛盾的结论。

**入场条件（不只看价位，还要有信号确认）**:

- 价格触及支撑区后，需要以下至少一个确认信号再入场：
  - 小时K线收阳（非假突破）
  - 成交量明显萎缩（卖盘枯竭）
  - RSI在支撑附近出现底背离
  - 30分钟信号由卖出转为买入

**风险收益比要求（每笔操作）**:

```
R:R = (目标收益) ÷ (止损距离) ≥ 1.5:1
例: 入场 1,162元/克，止损 1,153元/克（-9元），目标 1,179元/克（+17元）→ R:R = 1.9:1 ✅
```

#### 3.4 积存金点位换算 — 三级获取策略（详见 [accumulation-gold.md](references/accumulation-gold.md)）

首先计算理论换算比作为基准:

```
理论换算比 = USD/CNY汇率 ÷ 31.1035
理论换算价 = XAUUSD × 理论换算比
```

然后按优先级获取**实际换算比**:

**🥇 优先: 获取到银行积存金实际报价**

```
优先核验来源（默认先查这两个页面）:
- 浙商积存金: https://m.jdjygold.com/finance-gold/gold-standard/home/?productSku=1961543816
  XPath: //*[@id="mainCSC"]/div/div[2]/div
- 民生积存金: https://m.jdjygold.com/finance-gold/newgold/home/
  XPath: /html/body/div[1]/div/div[2]/div[3]/div[1]/div[1]

浙商换算比 = 浙商积存金报价 ÷ 当前XAUUSD
民生换算比 = 民生积存金报价 ÷ 当前XAUUSD
溢价 = 银行报价 − 理论换算价
⚠️ 强制合理性检验: 溢价（国内折溢价）通常在 -15 至 +25 元/克之间。若绝对值异常大（如>30元/克），说明抓取的报价有误（旧数据或不同产品）！必须暂停盲目计算，在报告中向用户核实验证真实盘口价格。
各点位积存金价 = XAUUSD关键点位 × 对应换算比
→ 标注: "基于银行实时报价换算"
```

**🥈 备选: 获取到上海金交所Au99.99价格（搜索SGE延时行情）**

```
SGE换算比 = Au99.99价格 ÷ XAUUSD（取SGE报价时对应的同期XAUUSD，避免时间错位）
溢价 = Au99.99价格 − 理论换算价
各点位积存金价 ≈ XAUUSD关键点位 × SGE换算比
→ 标注: "基于上海金交所Au99.99换算，积存金实际价格可能有±5元/克偏差"

⚠️ SGE时间错位警告: SGE日间盘15:30收盘，若当前时间在中国夜盘或美盘，
SGE数据已是小时前的数据。若XAUUSD较SGE收盘后又大幅变动（>$20），
则用SGE换算比偏差较大，应改用理论换算法并注明。
```

**🥉 兜底: 仅有理论换算（未获取到国内价格）**

```
各点位积存金价 = XAUUSD关键点位 × 理论换算比
→ 标注: "⚠️ 基于理论汇率换算，未包含溢价，实际价格可能偏差10-15元/克"
→ 建议用户自行查看银行APP确认
```

输出时同时展示:

- **国际金价** (XAUUSD): 美元/盎司
- **浙商积存金参考**: 人民币元/克
- **民生积存金参考**: 人民币元/克
- **换算依据**: 标注使用了哪级获取策略 + 当前换算比 + 溢价值

> **注意**: 积存金实际成交价由各银行实时报价决定，AI给出的是换算参考值。
> 国内金价与国际金价之间存在浮动溢价（通常 -15 ~ +15 元/克），非固定价差。

### 阶段4: 报告输出

**目标**: 生成结构化分析报告（短线操作导向，顶部展示**三轴综合倾向**与**执行门控**两行结论），**并将最终报告保存到 `assets` 文件夹中**。

1. **保存文件**: 每次分析结束后，必须将生成的报告内容保存到当前 Skill 目录下的 `assets/` 文件夹中，文件命名规则为 `assets/YYYY-MM-DD-HHmm-gold-analysis.md`。
2. 严格按报告模板输出 → [report-template.md](references/report-template.md)

## 数据校验与防幻觉机制（核心执行纪律）

为了提高分析的可靠性并提供高价值的参考，必须严格遵循以下信息溯源与验证机制：

1. **强制溢价溢出拦截**: 计算出的 `当前积存金价格` 与 `理论换算价` 之间的溢价值，一旦越界（绝对值 > 30元/克），则必定是抓取的信息出错（抓了期货价、老数据或抓错标的），此时系统必须截断并主动询问用户真实的盘口数字进行校准。
2. **拒绝幻觉造假**: 若某个参数（如某个特定银行报价、某个技术指标数值）最新网络确实搜不到对应时间节点数据，必须如实标明 `暂无最新明细` 或 `使用X小时前数据`。**绝对禁止用大模型内在历史经验或瞎编数值来填缝**。
3. **金钱与时间的绝对精确**: 行情分析的基础是时间和金钱。凡入档数据，务必审查出处的时间戳，坚决剔除搜索结果中“1天前”、“48小时前”的过期作废时效新闻。
4. **全程溯源化（URL索引）**: 最后输出的【数据来源】模块，无论是新闻还是报价出处，都必须添加 Markdown 格式的可点击 URL 链接，以便人类二次核对与评估信源权威性。
5. **主动探寻性**: 在搜集过程中，若发现“闪金闪跌（>1%波动）”等不寻常行情，必须立刻追加查询词汇（如"gold sudden drop reason"），不仅记录现象，更要主动探寻并剖析背后的根因（如某某大单砸盘、突发地缘冲击等）。
6. **银行页面优先核验**: 若用户已提供可访问的银行实时报价页面和XPath, 后续分析必须优先使用该页面抓取银行报价, 只有在页面不可用或字段歧义无法确认时才退回搜索/代理价格。

## 执行原则

### Do（要做）

- ✅ **必须将最终分析报告保存为文件**: 每次工作流的最后一步必须是将 Markdown 内容保存到 `assets/YYYY-MM-DD-HHmm-gold-analysis.md` 中。
- ✅ **首先 WebFetch investing.com/currencies/xau-usd**，获取实时 Bid/Ask + 多时间框架信号 + 经济日历，这是最快的数据来源
- ✅ 标注每条数据的获取时间（精确到分钟），数据超过30分钟视为过时
- ✅ 在报告顶部「当前状态速览」下展示**两行核心结论**：**综合倾向（三轴 AxisM/L/S）** + **执行门控（30m/1H）**；前者定方向预期，后者定当下是否可下单，避免用户误以为仅看短线信号
- ✅ 阶段1末尾运行 `fetch_jd_personal_latest_post.py`（路径见 `<SkillRoot>`），将金友圈写入报告并计入数据来源；失败则如实标注，不编造
- ✅ 所有点位同时展示国际金价(XAUUSD)和积存金换算价(元/克)
- ✅ 每笔交易必须给出止损位和风险收益比（R:R ≥ 1.5:1）
- ✅ 盘中使用 P=(今日高+今日低)÷2 计算实时轴心
- ✅ 当美元指数DXY单日波动 >0.5% 时，必须说明对国内积存金价格的影响（美元涨→积存金涨幅缩小；美元跌→积存金涨幅放大）
- ✅ 区分消息面的时效性（小时级别 vs 日级别）
- ✅ 报告末尾必须包含「五、数据来源」章节，逐一列出每条数据的来源并在平台名处**附加URL链接**
- ✅ 用户提供了银行报价URL + XPath时, 默认将其视为积存金实时报价的一号来源, 先核查再换算

### Don't（不要做）

- ❌ **不在执行门控为「不执行新开多/不抄底」时给出与门控矛盾的一键满仓建议**；门控为卖出时若三轴仍偏多，只能写「等待回调+门控改善」，不得写「立即追多」
- ❌ **不用延时数据（SGE等）直接换算当前积存金价**，若XAUUSD已大幅变动，SGE数据已失效，改用理论换算法并注明
- ❌ 不给出绝对化建议（如"必须买入"、"一定涨"）
- ❌ 不忽略积存金的买卖价差成本（通常0.5-1元/克），每笔分析都要计入
- ❌ 不硬套固定公式，要跟着当下资金情绪和实时信号走
- ❌ 不忽略"买预期卖事实"规律：重大利多消息落地后（如数据实际发布时），金价可能反向回调

## 参考文件

- 技术分析指标详细用法 → [technical-analysis.md](references/technical-analysis.md)
- 积存金产品知识与换算规则 → [accumulation-gold.md](references/accumulation-gold.md)
- 报告输出模板（含金友圈结构化呈现格式）→ [report-template.md](references/report-template.md)

